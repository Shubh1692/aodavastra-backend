import * as mongoose from "mongoose";
import { Address } from "./address.interface";
export declare const AddressSchema: mongoose.Schema<Address, mongoose.Model<Address, any, any, any, any>, {}, {}, any, {}, "type", Address>;
